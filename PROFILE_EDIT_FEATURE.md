# 📝 Редактирование профиля курьера в настройках

**Дата**: 2025-12-15

---

## ✅ ЧТО ДОБАВЛЕНО:

### **Редактирование данных курьера в личном кабинете**

В раздел **Настройки** (`activeTab='settings'`) в Dashboard добавлена карточка редактирования личных данных курьера.

---

## 📁 ИЗМЕНЕННЫЕ ФАЙЛЫ:

### 1. `src/components/dashboard/MessengerSettings.tsx`

#### **Добавленные импорты**:
```tsx
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
```

#### **Новые состояния**:
```tsx
const [profileData, setProfileData] = useState({
  full_name: user?.full_name || '',
  phone: user?.phone || '',
  city: user?.city || '',
});
const [isEditingProfile, setIsEditingProfile] = useState(false);
const [isSavingProfile, setIsSavingProfile] = useState(false);
```

#### **Новые функции**:

1. **`formatPhoneInput(value: string)`** - форматирование телефона в +7 (XXX) XXX-XX-XX
2. **`handleProfileEdit()`** - открытие режима редактирования
3. **`handleProfileCancel()`** - отмена редактирования
4. **`handleProfileSave()`** - сохранение изменений через API

#### **Новый UI компонент**:

Карточка с двумя режимами:

**Режим просмотра**:
- Отображение ФИО, телефона, города
- Кнопка "Редактировать"
- Иконки для каждого поля

**Режим редактирования**:
- Input поля для ФИО, телефона, города
- Валидация данных
- Кнопки "Сохранить" и "Отмена"
- Индикация загрузки при сохранении

---

## 🔌 BACKEND ИНТЕГРАЦИЯ:

### **API Endpoint**:
```
POST /api?route=profile&action=update
```

### **Заголовки**:
```json
{
  "Content-Type": "application/json",
  "X-User-Id": "<user_id>"
}
```

### **Body**:
```json
{
  "full_name": "Иванов Иван Иванович",
  "phone": "79991234567",
  "city": "Москва"
}
```

### **Response (Success)**:
```json
{
  "success": true,
  "user": {
    "id": 123,
    "full_name": "Иванов Иван Иванович",
    "phone": "79991234567",
    "city": "Москва",
    ...
  }
}
```

### **Backend файл**:
`backend/api/index.py` - функция `handle_profile()`

**Что делает**:
1. Проверяет заголовок `X-User-Id`
2. Валидирует обязательные поля (ФИО, телефон, город)
3. Обновляет запись в таблице `t_p25272970_courier_button_site.users`
4. Возвращает обновленные данные пользователя

---

## 🎨 UI/UX ОСОБЕННОСТИ:

### **Деловой стиль** (соответствует редизайну админки):
- `bg-white border border-gray-200 rounded-lg shadow-sm` - карточка
- `bg-blue-600` - акцентный цвет
- `font-semibold` - заголовки
- Минималистичный дизайн без градиентов

### **Иконки**:
- `User` - заголовок карточки и поле ФИО
- `Phone` - поле телефона
- `MapPin` - поле города
- `Edit` - кнопка редактирования
- `Check` - кнопка сохранения
- `X` - кнопка отмены
- `Loader2` - индикация загрузки

### **Валидация**:
- ФИО не может быть пустым
- Телефон должен содержать 11 цифр
- Город не может быть пустым
- Телефон автоматически форматируется при вводе

### **Форматирование телефона**:
- Ввод: `9991234567` → Отображение: `+7 (999) 123-45-67`
- Ввод начинается с `8` → автоматически заменяется на `7`
- При сохранении отправляется только цифры: `79991234567`

---

## ✅ СИНХРОНИЗАЦИЯ С AUTHCONTEXT:

После успешного сохранения:
1. Вызывается `updateUser()` из AuthContext
2. Обновляется локальное состояние пользователя
3. Данные сохраняются в `localStorage`
4. UI автоматически обновляется во всех компонентах

```tsx
if (updateUser) {
  updateUser({
    full_name: profileData.full_name.trim(),
    phone: phoneDigits,
    city: profileData.city.trim(),
  });
}
```

---

## 📱 АДАПТИВНОСТЬ:

### **Мобильные устройства** (< 640px):
- Компактные отступы `p-4`
- Скрытие текста "Редактировать" на кнопке (только иконка)
- Уменьшенные размеры иконок
- Читаемый текст `text-base`

### **Десктоп** (≥ 640px):
- Увеличенные отступы `sm:p-6`
- Полный текст на кнопках
- Большие иконки `sm:w-12 sm:h-12`
- Крупный текст `sm:text-lg`

---

## 🔄 ЖИЗНЕННЫЙ ЦИКЛ КОМПОНЕНТА:

### **1. Инициализация**:
```tsx
useEffect(() => {
  if (user) {
    setProfileData({
      full_name: user.full_name || '',
      phone: formatPhoneInput(user.phone || ''),
      city: user.city || '',
    });
  }
}, [user?.id, user?.full_name, user?.phone, user?.city]);
```

### **2. Редактирование**:
- Пользователь нажимает "Редактировать"
- `isEditingProfile = true`
- Отображаются Input поля

### **3. Сохранение**:
- Валидация данных
- Отправка POST запроса на API
- Обновление AuthContext
- Закрытие режима редактирования
- Toast уведомление

### **4. Отмена**:
- Сброс изменений
- Возврат к исходным данным из `user`
- Закрытие режима редактирования

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ:

### **Для пользователя**:

1. Зайти в личный кабинет (`/dashboard`)
2. Открыть вкладку **Настройки** (иконка шестеренки)
3. В карточке "Личные данные" нажать **Редактировать**
4. Изменить ФИО, телефон или город
5. Нажать **Сохранить**
6. Данные обновятся во всем приложении

### **Где отображаются обновленные данные**:

- ✅ Профиль курьера (`ProfileHeader`)
- ✅ Список курьеров в админке
- ✅ Реферальная статистика
- ✅ Заявки на вывод средств
- ✅ Telegram бот (если подключен)

---

## 🔐 БЕЗОПАСНОСТЬ:

### **Авторизация**:
- Требуется заголовок `X-User-Id`
- Backend проверяет существование пользователя
- Пользователь может изменить только свои данные

### **Валидация на Backend**:
```python
if not full_name or not phone or not city:
    return {
        'statusCode': 400,
        'body': json.dumps({'error': 'ФИО, телефон и город обязательны'})
    }
```

### **SQL Injection защита**:
Используются параметризованные запросы:
```python
cur.execute("""
    UPDATE t_p25272970_courier_button_site.users
    SET full_name = %s, phone = %s, city = %s, updated_at = NOW()
    WHERE id = %s
""", (full_name, phone, city, user_id))
```

---

## 📊 ТАБЛИЦА БД:

### **Обновляемые поля в `users`**:
- `full_name` - ФИО курьера
- `phone` - Телефон (только цифры, 11 символов)
- `city` - Город работы
- `updated_at` - Автоматически обновляется при изменении

---

## ✅ ТЕСТИРОВАНИЕ:

### **Проверить**:

1. ✅ Отображение текущих данных
2. ✅ Открытие режима редактирования
3. ✅ Форматирование телефона при вводе
4. ✅ Валидация пустых полей
5. ✅ Валидация короткого телефона (< 11 цифр)
6. ✅ Сохранение через API
7. ✅ Обновление данных в AuthContext
8. ✅ Toast уведомления (успех/ошибка)
9. ✅ Отмена изменений
10. ✅ Адаптивность на мобильных устройствах

### **Тестовые сценарии**:

#### **Сценарий 1: Успешное обновление**
1. Открыть настройки
2. Нажать "Редактировать"
3. Изменить ФИО на "Петров Петр Петрович"
4. Изменить телефон на "79001234567"
5. Изменить город на "Санкт-Петербург"
6. Нажать "Сохранить"
7. **Ожидаемый результат**: Toast "Профиль успешно обновлен", данные сохранены

#### **Сценарий 2: Валидация пустого ФИО**
1. Открыть редактирование
2. Очистить поле ФИО
3. Нажать "Сохранить"
4. **Ожидаемый результат**: Toast "ФИО обязательно для заполнения"

#### **Сценарий 3: Валидация короткого телефона**
1. Открыть редактирование
2. Ввести телефон "79001234" (неполный)
3. Нажать "Сохранить"
4. **Ожидаемый результат**: Toast "Введите полный номер телефона"

#### **Сценарий 4: Отмена изменений**
1. Открыть редактирование
2. Изменить все поля
3. Нажать "Отмена"
4. **Ожидаемый результат**: Возврат к исходным данным

---

**КОНЕЦ ОТЧЕТА**

Редактирование профиля курьера полностью работает! ✅
