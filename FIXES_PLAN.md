# 🔧 ПЛАН ИСПРАВЛЕНИЙ - Courier Button Site

**Дата**: 2025-12-15  
**Статус**: В работе

---

## 📋 ЗАДАЧИ:

### 1️⃣ **Профиль курьера - редактирование данных** ✅ УЖЕ РАБОТАЕТ
**Статус**: ✅ Функция есть и работает

**Где находится**:
- `src/components/ProfileSetupModal.tsx` - модальное окно редактирования
- `backend/api/index.py` → `handle_profile()` → `action=update`

**Что редактируется**:
- ФИО (full_name)
- Телефон (phone)
- Город (city)
- Реферальный код (только при первом заполнении)

**API эндпоинт**:
```
POST https://functions.poehali.dev/5f6f6889-3ab3-49f0-865b-fcffd245d858?route=profile&action=update
Headers: X-User-Id
Body: {full_name, phone, city, referral_code_input?}
```

**Проблема**: Нужно проверить, что обновление сохраняется в БД.

---

### 2️⃣ **Админка - 44 курьера (дубликаты из CSV)**
**Статус**: ❌ ПРОБЛЕМА НАЙДЕНА

**Причина**: 
- Таблица `couriers` имеет 44 записи
- Это игровые аккаунты для 3D симулятора, а НЕ реальные курьеры!
- Структура таблицы:
  ```sql
  id, username, created_at, total_deliveries, total_coins, 
  total_distance, level, experience, current_vehicle, 
  user_id, phone, first_name, last_name, city
  ```

**user_id** - это ID реального пользователя из авторизации

**Проблема**: Backend использует таблицу `couriers` как список курьеров сайта, но это ИГРОВЫЕ данные!

**Решение**:
1. Найти **реальную таблицу пользователей** (скорее всего, нет отдельной таблицы)
2. Создать VIEW или использовать OAuth данные из `couriers` где `user_id IS NOT NULL`
3. Исправить backend админки

---

### 3️⃣ **CSV загрузка - работает неправильно**
**Статус**: ❌ ТРЕБУЕТ ИСПРАВЛЕНИЯ

**Что делает сейчас**:
- Загружает CSV с данными курьеров из Яндекс.Еда
- Создает записи в `courier_earnings`, `courier_earnings_snapshot`
- Пытается связать с курьерами по `external_id`

**Проблема**:
- CSV создает НОВЫЕ записи вместо обновления существующих
- Нет связи между `couriers` (игра) и реальными курьерами
- `external_id` не используется как первичный ключ

**Что ДОЛЖНО быть**:
1. CSV загружается для **существующих курьеров**
2. Обновляет `courier_earnings_snapshot`:
   - `last_known_amount` - заработок из CSV
   - `last_known_orders` - количество заказов из CSV
3. НЕ создает новых записей
4. Показывает:
   - Найдено в базе: X курьеров
   - Обновлено: Y записей
   - Не найдено в базе: Z записей

**Решение**:
- Переписать логику CSV загрузки
- Использовать `phone` или `external_id` для поиска курьера
- Только UPDATE, никаких INSERT

---

### 4️⃣ **Telegram бот - проверка и исправление**
**Статус**: ❌ ТРЕБУЕТ ПОЛНОГО РЕФАКТОРИНГА

**Текущая структура**:
```
backend/telegram-bot/index.py - обработчик вебхуков
backend/messenger-api/index.py - API для привязки
```

**Что делает сейчас**:
1. Привязка через код (6 символов, 10 минут)
2. Команды: /start, /stats, /games, /withdrawals
3. Кнопки: 🎮 Игры, 📊 Статистика, 💰 Выплаты
4. Web App для игр

**Проблемы**:
1. ❌ Нет обработки ошибок привязки
2. ❌ Коды истекают, но не удаляются
3. ❌ Нет проверки дубликатов Telegram ID
4. ❌ Статистика показывает неправильные данные
5. ❌ Кнопки меню не адаптивны

**Что ДОЛЖНО быть**:
1. ✅ Привязка через код (как есть)
2. ✅ Проверка истёкших кодов
3. ✅ Красивое меню с актуальными данными
4. ✅ Уведомления о выплатах (работает?)
5. ✅ Команды для отладки (для админа)

---

## 🔍 АНАЛИЗ СТРУКТУРЫ БД:

### **Реальные курьеры сайта - ГДЕ?**

**Вариант 1**: OAuth пользователи в `couriers` где `user_id IS NOT NULL`
```sql
SELECT * FROM couriers WHERE user_id IS NOT NULL
```

**Вариант 2**: Нет отдельной таблицы, только OAuth сессии

**Вариант 3**: Таблица `users` (но её НЕТ)

---

### **Таблицы для CSV**:

1. **courier_earnings** - история заработка
   ```sql
   courier_id, external_id, total_amount, orders_count, created_at
   ```

2. **courier_earnings_snapshot** - последние данные
   ```sql
   id, courier_id, external_id, last_known_amount, last_known_orders, last_updated
   ```

3. **courier_vehicles** - транспорт курьеров
   ```sql
   courier_id, vehicle_type, vehicle_model, external_id
   ```

**Проблема**: `courier_id` ссылается на `couriers.id`, но `couriers` - это игровые данные!

---

## 🎯 ПЛАН ДЕЙСТВИЙ:

### Шаг 1: Найти реальную таблицу пользователей
```sql
-- Проверить couriers с user_id
SELECT COUNT(*) FROM couriers WHERE user_id IS NOT NULL;

-- Проверить структуру
SELECT * FROM couriers WHERE user_id IS NOT NULL LIMIT 1;
```

### Шаг 2: Создать правильную структуру
Если нет таблицы реальных курьеров:
```sql
CREATE TABLE real_couriers (
  id SERIAL PRIMARY KEY,
  oauth_provider VARCHAR,
  oauth_id VARCHAR,
  full_name VARCHAR,
  phone VARCHAR,
  city VARCHAR,
  email VARCHAR,
  referral_code VARCHAR UNIQUE,
  invited_by_user_id INT,
  created_at TIMESTAMP DEFAULT NOW(),
  avatar_url VARCHAR
);
```

### Шаг 3: Исправить CSV загрузку
- Искать по `phone` в `real_couriers`
- Обновлять `courier_earnings_snapshot`
- Показывать статистику загрузки

### Шаг 4: Исправить админку
- Использовать `real_couriers` вместо `couriers`
- Показывать только реальных пользователей
- Добавить редактирование

### Шаг 5: Исправить Telegram бота
- Обработка ошибок
- Красивое меню
- Актуальная статистика

---

## 🚨 КРИТИЧЕСКИЕ ВОПРОСЫ:

1. **Где хранятся реальные курьеры сайта?**
   - В `couriers` с `user_id IS NOT NULL`?
   - В отдельной таблице?
   - Только OAuth без постоянного хранения?

2. **Что такое `couriers.user_id`?**
   - FK на таблицу users? (но её НЕТ)
   - ID из OAuth провайдера?
   - Связь с игровым аккаунтом?

3. **Для чего нужна CSV загрузка?**
   - Обновление заработка из Яндекс.Еда?
   - Синхронизация заказов?
   - Расчет бонусов?

4. **Как работает реферальная система?**
   - `invited_by_user_id` в какой таблице?
   - Кто получает бонусы?
   - Где хранятся выплаты?

---

**СЛЕДУЮЩИЙ ШАГ**: Проверить структуру БД и найти реальную таблицу пользователей.
